# 工作记录 — 2026-07-12

## 今日主题：游戏背景图替换灰色背景 + 局部背景亮度 + 前景/背景UI分离控制 + ICtCp域替换YCbCr + 两分支对比

---

## 1. 纯灰背景改为游戏背景图

### 背景

原 HDR/SDR 窗口背景为纯灰（HDR: `bgNit` nit 均匀灰，SDR: 18% gray @ 350nit = 63nit）。纯灰背景无纹理内容，无法测试 UI 在真实游戏画面上的主观一致性。

### 修改

| 文件 | 改动 |
|---|---|
| `CMakeLists.txt` | 新增 `BG_IMAGE_DIR` 编译定义指向 `pic/` 目录 |
| `src/texture.h/cpp` | 新增 `loadBackgroundTexture()` 加载 PNG → `VK_FORMAT_R8G8B8A8_SRGB` 纹理，计算平均亮度 |
| `src/vulkan_util.cpp` | descriptor layout 2→3 bindings（加 `binding=2 texBG`） |
| `shaders/hdr_ui.frag` / `sdr_ui.frag` | 加 `binding=2 texBG`，全屏 quad 采样背景纹理 |
| `src/hdr_app.cpp` / `sdr_app.cpp` | 加载背景纹理，更新 descriptor set 写入 binding 2 |

全屏 quad：vertex `scale=(2,2) offset=(0,0)`（顶点 ±0.5 × 2 = ±1 NDC = 全屏）。shader 通过 push constant 的 `uiOffset`/`uiScale` 计算 UI 区域 UV，UI 区域外只显示背景。

提交：`e9c718b feat: 游戏背景图替换灰色背景,支持亮度调节`

---

## 2. HDR 背景亮度系数 Bug：BT.709 luma vs BT.2020 luma

### 问题

CPU 用 BT.709 luma（0.2126/0.7152/0.0722）计算背景平均亮度 = 124.31 nit，但 HDR shader 把背景转成 BT.2020 nit 后乘 multiplier，显示器按 BT.2020 luma（0.2627/0.6780/0.0593）感知亮度 = 129.83 nit。BT.709→BT.2020 矩阵有舍入误差，两者不一致。

实际影响：BG Nit=1000 时实际输出 1044 nit（偏高 4.4%）。

### 原理

BT.709→BT.2020 转换矩阵理论上是保亮度（luminance preserving）的，即 `Y_2020 = Y_709`。但矩阵系数截断到有限精度后，行和不再精确等于 luma 系数，导致 `Y_2020 ≠ Y_709`。

### 修改

`loadBackgroundTexture()` 增加 `bool hdr` 参数：
- HDR：`sRGB→linear→×350→BT.2020 矩阵→BT.2020 luma` = 129.83 nit
- SDR：`sRGB→linear→BT.709 luma` = 124.31 nit（不变）

提交：`74c6ac4 fix: HDR背景亮度系数用BT.2020 luma计算,修复4.4%偏差`

---

## 3. 前景/背景 UI 分离控制

### 问题

UI（如 `1_1839_724`）包含两部分：
- **前景 UI**：亮度高、不透明度高（alpha > 0.5）
- **背景 UI**：亮度低、不透明度低（alpha ≤ 0.5）

用同一组 Eff.Alpha 和 Y-Scale 调节时：
- 低 alpha 像素对 BG Nit 变化更敏感（`Δmixed = (1-α)·Δbg`，α=0.3 时系数 0.70 vs α=0.9 时 0.10，差 7 倍）
- 高亮度像素对 Y-Scale 更敏感（PQ 域非线性，350nit 放大 19x vs 10nit 放大 5.5x）
- 纯灰背景下看不出此差异（无纹理内容透过），纹理背景暴露了不一致

### 修改

| 文件 | 改动 |
|---|---|
| `shaders/hdr_ui.frag` | `texAlpha > 0.5` → 前景组（`fgAlpha`/`fgYScale`），≤ 0.5 → 背景组（`bgAlpha`/`bgYScale`） |
| `shaders/sdr_ui.frag` | 同上，前景/背景各用独立 alpha |
| push constant | 48B→56B（新增 `bgAlpha` + `bgYScale`） |
| `src/hdr_app.h/cpp` | `effAlpha_`/`yScale_` → `fgAlpha_`/`bgAlpha_`/`fgYScale_`/`bgYScale_` |
| `src/sdr_app.h/cpp` | `sdrAlpha_` → `sdrFgAlpha_`/`sdrBgAlpha_` |
| ImGui | 两组滑动条：FG Eff.Alpha/FG Y-Scale + BG Eff.Alpha/BG Y-Scale + 共享 CbCr-Scale |

CbCr-Scale 保持共享（用户确认）。前景判定条件 `alpha > 0.5`（用户确认）。

提交：`5c5c94b feat: 前景/背景UI分离控制`

---

## 4. 局部背景亮度（从全局平均改为局部平均）

### 问题

用整张背景图的**全局平均亮度**作为 multiplier 校准基准。极端情况：画面 90% 暗室 + 10% 高光，UI 叠在高光区域中心，全局平均可能 100nit 但 UI 周围局部亮度 500nit。用 100nit 校准 UI，在 500nit 高光背景上 UI 偏暗。

### 原理

人眼适应的是**局部**亮度（local adaptation），不是全局平均。同时对比效应（simultaneous contrast）决定 UI 的感知对比度取决于其紧邻背景。文献调研结论：
- 最强适应区域在目标周围 1-5° 视觉角（Barten CSF, Allred et al. JOV 2012）
- 色调映射文献（Reinhard, Ashikhmin, Pattanaik）用边缘感知多尺度方法
- 无统一固定角度，工程上取 UI 尺寸的 1.5-2 倍线性

### 方案

局部区域 = UI quad 线性尺寸 × 2（面积 × 4），居中，clamp 到 [0,1]。

### 修改

| 文件 | 改动 |
|---|---|
| `src/hdr_app.h/cpp` | 新增 `bgRawRGBA_`/`bgWidth_`/`bgHeight_`/`localAvgNit_`，`computeLocalAvgNit()` 方法 |
| `src/sdr_app.h/cpp` | 同上 |
| `loadBackgroundTexture()` | 增加 `std::vector<uint8_t>& rawRGBA` 输出参数 |
| `drawFrame()` | multiplier = `bgNit_ / localAvgNit_`（原 `bgAvgNit_`） |
| shader | local 区域内绘制背景纹理，local 外输出黑色（硬边界） |
| 触发时机 | init / 切换 UI / 窗口 resize 时重新计算 |

### Bug 修复

1. **`computeLocalAvgNit()` 在 `initWindowSwapchain()` 之前调用**：`swapchainExt={0,0}` → `frac=inf` → local 区域 = 全图 → `localAvgNit=globalAvg`。修复：移到 `initWindowSwapchain()` 之后。
2. **HDR 初始 `bgNit_` 用全局 avg 而非局部 avg**：修复：`computeLocalAvgI()` 后设 `bgNit_ = (int)localAvgI_`。

提交：`1d92bdb`, `3b68eba`, `65bbd86`, `8311871`

---

## 5. ICtCp 域替换 YCbCr 域

### 问题

YCbCr 的 Y 分量基于 PQ 编码后的 RGB 加权（`Y = 0.2627·R_pq + 0.6780·G_pq + 0.0593·B_pq`），是 BT.2020 luma 在 PQ 域的线性组合。但 Y 不是感知均匀的——不同亮度级别的相同 ΔY 产生不同的感知差异。

纹理背景线性 avg=1000nit 时，感知等效仅 ~608nit（Jensen 不等式：`avg(PQ(Y_i)) < PQ(avg(Y_i))`，因为 PQ 是凹函数）。纯灰 1000nit 的感知亮度 = `PQ(1000)`，但纹理图的感知平均 = `avg(PQ(Y_i))` < `PQ(1000)`。两者相差 39%。

### 原理

ICtCp（ITU-R BT.2100）是感知均匀色彩空间：
- **I（Intensity）**：`I = 0.5·L'_PQ + 0.5·M'_PQ`，其中 L'/M' 是 PQ 编码后的 LMS 锥响应
- I 的等距步长对应等距感知差异（基于 Barten CSF）
- 对灰像素：`I = PQ(nit)`（因为 L=M，归一化后 L=M=S）

### 转换管线

```
BT.2020 linear RGB (nit)
→ LMS (normalized: RGB2LMS / LMS_white, 使灰→L=M=S)
→ ×10000 → PQ encode
→ ICtCp (3×3 matrix)
→ I × iScale, Ct × ctCpScale, Cp × ctCpScale
→ inverse ICtCp → LMS(PQ) → PQ decode → LMS → RGB (nit)
→ 线性域与 BG 混合 → PQ encode → 输出
```

LMS 归一化：原始 RGB2LMS 矩阵对 D65 白点不满足 L=M=S，需每行除以对应的 LMS 白点值。归一化后灰像素 → L=M=S → I=PQ(nit)，Ct=Cp=0。

### 修改

| 项目 | 旧（YCbCr） | 新（ICtCp） |
|---|---|---|
| UI 调整域 | PQ YCbCr (Y/Cb/Cr) | PQ ICtCp (I/Ct/Cp) |
| 亮度滑动条 | Y-Scale | I-Scale |
| 色度滑动条 | CbCr-Scale | CtCp-Scale |
| BG multiplier | `bgNit_ / localAvgNit_`（线性域） | `PQ(bgNit_) / localAvgI_`（PQ域） |
| localAvg 计算 | avg(BT.2020 luma) 线性域 | avg(I) PQ域 |
| SDR | 不变 | 不变 |

push constant 布局不变（56B），仅字段语义改变。

### Bug 修复

**CPU `computeLocalAvgI()` 矩阵 M 行第三列值写错**：

```cpp
// 错误：0.08044665 是 S 行第二列的值
float M = -0.19536979f*r2 + 1.11868999f*g2 + 0.08044665f*b2;
// 正确：0.07667981
float M = -0.19536979f*r2 + 1.11868999f*g2 + 0.07667981f*b2;
```

影响：CPU M 行 row sum = 1.0038（应 1.0），gray 350nit 时 CPU 算 M=351.32（应 350），localAvgI 偏高 0.03%，multiplier 偏小，BG Nit=1000 时实际输出 997.8nit（比 YCbCr 暗 2.2nit）。在 BVM X3110 WFM 上可见。

同时删除未使用的 S 行计算（I = 0.5L'+0.5M'，S 不参与）。

提交：`e5ab898`, `4f302d9`

---

## 6. 背景图片切换功能

### 需求

HDR/SDR 窗口各加 "Next BG" 按钮，循环切换背景图片。循环列表 = `pic/` 下所有 `*_rotate.png`（按字母排序）+ 全白背景（sRGB 255,255,255 = 350nit/通道）。切换后重新计算 localAvg，BG Nit 保持不变。

### 修改

| 文件 | 改动 |
|---|---|
| `src/texture.h/cpp` | 新增 `createSolidTexture()` 创建纯色纹理 |
| `src/hdr_app.h/cpp` | 单 bg 成员 → `vector<UITexture> bgTextures_` 等，`switchBackground()` / `updateBGDescriptorSets()` |
| `src/sdr_app.h/cpp` | 同上 |
| ImGui | "Next BG" 按钮，显示当前 BG 名称和序号 |

白色纹理 64×64（非 1×1，避免 `computeLocalAvgI` 中空像素范围 bug）。

### Bug 修复

1. **白色纹理 1×1 导致 `computeLocalAvgI` 空范围**：`bx0=0, bx1=0` → early return → `localAvgI=0` → `multiplier=0/0=NaN` → 全黑。修复：改为 64×64 + fallback 全图范围。
2. **MSVC 文件扫描失败**：`std::transform` + `::tolower`（未 `#include <cctype>`）在 MSVC 上行为异常，`_rotate.png` 后缀匹配失败。修复：改用 `fn.find("_rotate.png")`。
3. **白色纹理内联 Vulkan 代码**：60 行 `extern` 声明 + 手动 staging buffer，在 MSVC 上可能有链接问题。修复：提取为 `createSolidTexture()` 函数。

提交：`994f57c`, `e1ffb73`, `f9761e8`, `2609885`

---

## 7. 两分支对比方案

### 目的

对比 ICtCp 域 vs YCbCr 域的背景亮度主观感受，验证 ICtCp 的 I 分量是否比 YCbCr 的 Y 分量更接近纯灰背景的感知亮度。

### 两个分支

| 分支 | 管线 | multiplier 域 | 亮度调节域 |
|---|---|---|---|
| `chromaScale_YUV_GameBG` | YCbCr (Y/Cb/Cr) | 线性域 `bgNit_/localAvgNit_` | Y-Scale (PQ YCbCr) |
| `chromaScale_ICtCp_GameBG` | ICtCp (I/Ct/Cp) | PQ域 `PQ(bgNit_)/localAvgI_` | I-Scale (PQ ICtCp) |

两分支共享：背景切换、前景/背景分离、local 区域、SDR 管线。仅 HDR 管线不同。

### 物理含义对比

| | YCbCr 分支 | ICtCp 分支 |
|---|---|---|
| BG Nit=1000 含义 | 线性 avg(Y) = 1000nit | avg(I) = PQ(1000) |
| 灰背景 | 每像素 1000nit ✓ | 每像素 1000nit ✓ |
| 纹理背景 | avg(线性Y) = 1000nit | avg(I) = PQ(1000)，avg(线性Y) < 1000nit |
| 感知匹配纯灰 | 否（偏暗 39%） | 是（I 感知均匀） |

### 编译注意事项

两分支需独立 build 目录（`build_ycbcr/` 和 `build_ictcp/`），因 shader .spv 文件不兼容（push constant 布局相同但 shader 逻辑不同）。

---

## 8. 死代码清理

删除两个分支共有的死代码：

| 删除项 | 说明 |
|---|---|
| `computeAvgMixedNit()` | hdr_app 中声明+定义但从未调用的函数 |
| `src/main.cpp` (1723行) | 不在任何 CMake 编译目标中的遗留文件 |
| `shaders/ui.frag` | 仅被 main.cpp 引用 |
| `#include <cstring>` | ICtCp 分支独有，未使用 |

提交：`84e3394` (ICtCp), `2331b21` (YCbCr)

---

## 今日提交汇总

| 提交 | 分支 | 说明 |
|---|---|---|
| `e9c718b` | YCbCr→ICtCp | 游戏背景图替换灰色背景 |
| `74c6ac4` | YCbCr→ICtCp | HDR亮度系数用BT.2020 luma |
| `5c5c94b` | YCbCr→ICtCp | 前景/背景UI分离控制 |
| `1d92bdb` | YCbCr→ICtCp | 局部平均替代全局平均 |
| `65bbd86` | YCbCr→ICtCp | 只绘制local区域背景 |
| `8311871` | YCbCr→ICtCp | bgNit_初始值用localAvg |
| `e5ab898` | ICtCp | HDR管线YCbCr→ICtCp |
| `994f57c` | ICtCp | 背景图片切换功能 |
| `e1ffb73` | ICtCp | 白色纹理64×64+createSolidTexture |
| `f9761e8` | ICtCp | 文件扫描调试输出 |
| `2609885` | ICtCp | find替代tolower |
| `35dc332` | YCbCr | YCbCr分支添加背景切换 |
| `84e3394` | ICtCp | 死代码清理 |
| `2331b21` | YCbCr | 死代码清理 |
| `4f302d9` | ICtCp | CPU矩阵M行值修复 |
