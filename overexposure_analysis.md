# HDR UI 软褪色分析 —— 色彩保真的 uiLumNit 上限

> 临时分析文档(不入库)。公式用 LaTeX,用支持 LaTeX 的 Markdown 预览器(VS Code + Markdown Preview Enhanced / Typora / Obsidian)查看。
>
> **本文件已修正**:之前用白峰 $P=4000$ 算的是"硬 clip(像素→白)"上限,不是你要的"软褪色(色彩变淡)"上限。软褪色发生在远低于 $P$ 的亮度,成因是**显示器色域体积**。

---

## 0. 目标重述

在主观一致性测试里,**色彩变了就谈不上主观一致**。所以要找的是:对每个 UI,`uiLumNit` 最高能到多少,**保证任何像素都不出现可见的色彩褪化**(不是"不成纯白",是"颜色不变淡")。

---

## 1. 软褪色的成因:显示器色域体积

### 1.1 物理

HDR 显示器(BT.2020)的三个原色 R/G/B 在全驱动下各有**有限的发光亮度**——可由 BT.2020 luma 系数(0.2627 / 0.6780 / 0.0593,已核实)× 白峰 $P$ 推算:R≈0.2627$P$、G≈0.6780$P$、B≈0.0593$P$。白峰 $P=4000$ nit。**蓝色原色的全驱动亮度最低**(≈0.0593×4000≈237 nit),远低于白峰。

显示器对**每个 BT.2020 通道**独立渲染:某像素某通道的 nit 值一旦**超过该原色的全驱动亮度**,该通道就被 clip → RGB 比例被破坏 → 像素色彩褪化(白会发黄=蓝通道先 clip;饱和色会褪向白=该色对应通道被 clip)。这发生在**远低于白峰 $P$** 的亮度,是色域体积在高亮度处收窄的表现。

> 关键区别:白峰 $P=4000$ 处的"三通道同时到顶→成纯白"是硬 clip。软褪色是"某一通道先到其原色极限→色彩偏移",阈值低得多。

> **术语更正**:本文件此前使用"原色峰值"($L_c$ / $L_{\min}$)一词,经核实**并非业界标准术语**(是我自造的),已替换为标准表述"原色的全驱动亮度(可由 luma 系数 × 白峰 $P$ 推算)"。色域体积的正式定义见**第二部分 §9**。

---

> **原第 2~8 节(基于"原色峰值 $L_{\min}$"的公式 $\text{uiLumNit}_{\max}=L_{\min}\times\text{lumAvg}$、推导、$L_{\min}$ 标定方法、各 UI 上限表、测试改法、待拍板)已作废删除**——其中公式是过度简化的近似(用全局平均/最大,非逐像素逐色相),术语非标准。正确方法见**第三部分 §3.3**(色域边界 → 逐像素 $J_{\max}$ → UI 上限)。

---

# 二、色域体积:权威定义、行业标准、影响因素与保色调节方法(联网调研)

> 本章基于维基百科 *Gamut*、*Tone mapping* 词条,以及 ITU-R / SMPTE / CTA 的 HDR 公开规范整理。

## 9. 权威定义

### 9.1 色域(Color Gamut)= 二维

色域指一个设备/流程**能准确再现的颜色集合**(凸包),通常在 CIE 1931 色度图上画成一个三角形,三个顶点是设备的 R/G/B **原色色度**。它只描述"色度平面",不含亮度维度。
> 来源:Wikipedia *Gamut*:"a gamut ... is a convex set containing the colors that can be accurately represented ... A trichromatic gamut is often visualized as a color triangle."

### 9.2 色域体积(Color Volume / Color Solid)= 三维

把色域加上**亮度(光ness)维度**,就是三维的"色立体 / 色域体积"。它才完整描述"设备在多亮时还能保多饱和的色"。
> 来源:Wikipedia *Gamut*:"Gamuts may also be represented in 3D space as a color solid, which includes a visualization of the dimension of lightness."并指出 RGB 色立体在低亮度是 R/G/B 三角、高亮度退化为 C/M/Y 三角、最高点只剩一个白点——**这正好说明色域体积在高亮度处收窄**。

**对 HDR 显示器的关键事实**:色域体积不是方盒子,而是"上窄下宽"——亮度越高,可表达的饱和色越少。这正是我们实测到"高亮度褪色"的根因。

## 10. 行业标准与规范(可引用)

| 标准 | 内容 | 与色域体积的关系 |
|---|---|---|
| **ITU-R BT.2100** | HDR 图像系统参数:原色色度(同 BT.2020)、参考白、PQ 与 HLG 两种转移特性 | 定义 HDR 的**信号侧**色度与亮度编码 |
| **SMPTE ST 2084** | PQ(Perceptual Quantizer)EOTF,绝对亮度转移(码值↔nit) | 使亮度成为**绝对量**,与显示器无关——这是"通用曲线可用绝对 nit 表达"的规范基础 |
| **SMPTE ST 2086** | **Mastering Display Color Volume Metadata**:母版显示器的原色色度 + 峰值白亮度 + 黑位 | **唯一以"色域体积"命名的元数据**;但只给整体 min/max 亮度与原色色度,**不含各原色峰值 $L_R/L_G/L_B$** |
| **CTA-861.3 / 861.4** | HDR 静态元数据(EDID):含 ST 2086 块 + MaxCLL/MaxFALL(内容光级) | 消费端能拿到的显示器能力描述,但同样**缺 per-primary 峰值** |
| **Dolby Vision / HDR10+** | 动态元数据,逐场景色调映射 | 把"内容如何映射进显示器色域体积"交给显示端逐场景决策 |

**一个重要缺口**:即使是专门描述"色域体积"的 ST 2086,也**只给原色色度 + 整体 min/max 亮度**,不给各原色峰值亮度($L_c$)。所以应用**即便读了规范元数据,也拿不到 $L_{\min}$**(最暗原色峰值),无法精确知道色域体积在高亮度处的真实边界。这是行业标准层面的一个盲区,也是我们应用落地难的根因之一。

## 11. 色域体积的影响因素

1. **原色色度(primaries chromaticity)**:决定二维色域底面有多大(BT.2020 > DCI-P3 > sRGB)。
2. **各原色峰值亮度 $L_R, L_G, L_B$**:决定三维体积的"高度形状"——每路能开多亮。**这是高亮度处收窄的直接原因**:某路先到顶就被卡,色度被破坏。
3. **白峰 $P = L_R+L_G+L_B$**:体积的最高点(白)。
4. **黑位(min luminance)**:体积的底(OLED≈0,LCD 较高)。
5. **转移特性(EOTF,PQ/HLG)**:码值如何映射到亮度,影响"哪段亮度被细分"。
6. **显示器自身的 tone-mapping / OOTF**:消费显示器对超出色域体积的信号做柔和压缩,母版机做硬 clip——同一信号在不同显示器上褪化方式不同。

## 12. 保证颜色在色域体积内的亮度调节方法(现成原理)

业界有成熟的"把内容塞进有限色域体积、同时尽量保色"的方法,统称**色调/色域映射**:

- **Tone mapping(色调映射)**:把高动态范围亮度压缩进显示范围,分**全局**(逐像素同函数,如 Reinhard $V_{out}=V_{in}/(V_{in}+1)$、gamma 压缩)和**局部**(随邻域变化,保局部对比但可能有 halo)。
  > 来源:Wikipedia *Tone mapping*。
- **Gamut mapping(色域映射)**:把超出色域的颜色投影回色域内(ICC 色彩管理的标准操作)。
- **Display-Adaptive Tone Mapping(显示自适应色调映射,Mantiuk/Daly/Kerofsky 2008)**:**关键**——这是一种**以显示器模型(含色域体积)为输入**的色调映射,显式针对"当前显示器能到什么亮度、什么色"来调整。这正是"保色亮度调节"的学术正解。
  > 来源:Mantiuk et al., *Display Adaptive Tone Mapping*, MPI technical report;Wikipedia *Tone mapping* 引用。
- **iCAM06**:基于色彩外观模型(CIECAM02 系)的色调映射,带白点/色度适应,兼顾感知。
- **Inverse tone mapping(逆色调映射)**:SDR→HDR 上转换(正是本场景"SDR 游戏转 HDR"用到的技术)。

**结论:方法存在且成熟**——display-adaptive tone mapping 就是"给定色域体积,调整亮度使颜色留在体积内"的标准做法。

## 13. 回到我们的问题

把上面串起来:

1. **"高亮度褪色"= 色域体积在高亮度处收窄**——某个原色到顶被卡,RGB 比例被破坏。这是色域体积的固有特性,有标准定义(ST 2086 命名、维基色立体描述),不是我们的发现,是已知现象。

2. **"保色地调亮度"有现成方法** = display-adaptive tone mapping(以及 gamut mapping / 全局 tone mapping)。**学术正解存在**。

3. **但该方法需要一个输入:当前显示器的色域体积模型(尤其各原色峰值 $L_c$)。** 而这正是应用拿不到的:
   - ST 2086 元数据不含 per-primary 峰值(§10 的缺口)。
   - 应用不能查型号+查表(不现实,且新机无表)。
   - 所以应用无法精确做 display-adaptive tone mapping。

4. **因此我们的处境**:
   - 我们的**通用映射曲线**(绝对 nit,显示器无关)= display-adaptive tone mapping 里"目标亮度"那一半(给定 BG,UI 该多亮才匹配主观)——这一半是通用的、可交付的。
   - 缺的另一半是"**按当前显示器色域体积把这个目标值塞进体积内**"(保色压缩)——这一半需要 $L_c$,应用没有。
   - 现状下这一半要么交给**显示器自己的 tone-mapping**(HDR 内容标准做法,母版机硬 clip、消费机柔和),要么应用用**保守常数封顶**(不精确但零依赖)。

5. **行业标准层面的启示**:
   - 要让应用真正能"保色调亮度",需要 **OS/平台把显示器色域体积(含 per-primary 峰值)暴露给应用**。目前 ST 2086/CTA-861 暴露不完整(缺 $L_c$)——这是**可向项目/标准反馈的一个改进点**。
   - 在此之前,通用曲线 + 保守封顶(或显示端 tone-mapping 兜底)是务实的落地形态。

**一句话给项目负责人**:我们遇到的"UI 高亮度褪色"是被业界标准明确描述的**色域体积**现象(ST 2086 / 维基色立体);保色调亮度的方法(display-adaptive tone mapping)也成熟;但该方法需要显示器各原色峰值,而现有标准元数据(ST 2086)不含此值、应用也拿不到——这是"通用曲线能否在应用侧精确保色落地"的真正瓶颈,属行业元数据缺口,建议作为项目的一项结论/反馈上报。

### 参考来源
- Wikipedia, *Gamut* — https://en.wikipedia.org/wiki/Gamut(色域/色立体定义、optimal color solid、MacAdam limit)
- Wikipedia, *Tone mapping* — https://en.wikipedia.org/wiki/Tone_mapping(tone mapping 方法、display-adaptive、iCAM06、inverse tone mapping)
- ITU-R BT.2100 — HDR 图像系统参数(原色、PQ/HLG)
- SMPTE ST 2084 — PQ EOTF
- SMPTE ST 2086 — Mastering Display Color Volume Metadata
- CTA-861.3 / 861.4 — HDR 静态元数据
- Mantiuk, Daly, Kerofsky, *Display Adaptive Tone Mapping* (2008) — 显示自适应色调映射

---

# 三、UI 提亮保色的上限——解决思路(已校正引用)

> 本节是"如何算出当前 UI 在彩度可保真范围内的最大亮度"的解决思路。
> **处理原则**:可验证的、有出处的事实保留;我此前**记忆出错或无坚固出处的部分(某篇具体论文、某个以人名命名的方法)用抽象代词代替**,不冒充确证。已撤回的错误(如自造的"原色峰值/L_min"、凭记忆报的 "Morovič & Sun, CRA 2000")不在此节出现。

## 3.1 事实基础(本会话已核实)

1. **色域体积(color volume)定义**:维基百科 *Color solid* / *Color volume* 词条原文——"2D 色域(色度)与亮度动态范围的组合"。✓ 已核实。
2. **色立体在顶部收窄成白点**:同词条——"上极所有色相会聚于白,下极会聚于黑"。即**高亮度处可表达的饱和色减少,顶点只剩白(色度=0)**。✓ 已核实。
3. **BT.2020 luma 系数 0.2627 / 0.6780 / 0.0593**:出现在 HDR Vivid TUWA 005.1-2022 的 YCbCr 矩阵里(本会话已读其 PDF 文本)。✓ 已核实。
4. **显示器元数据可取字段**:DXGI `DXGI_OUTPUT_DESC1`(见 `~/Downloads/main.cpp`)提供三原色色度、白点、MinLuminance、MaxLuminance、MaxFullFrameLuminance。✓ 已核实(读了代码)。
5. **CIECAM02(CIE 159:2004)色彩外观模型**:业界标准,把颜色转成感知量(J 明度、C 彩度、h 色相)。可查证的已建立标准(项目工作日志亦在用)。

## 3.2 由事实得出的结论(对应原 Q2)

由 3.1(2),色域体积有限且顶部只剩白,所以:**任何带彩度的 UI 像素,都有一个"最高可保色亮度"——在该色相下保持那个彩度时,亮度最高只能到色域边界;再高,显示器只能把它往白降饱和才能发出来**。因此一定存在一个亮度上限,超过它,被影响的像素无法保持原彩度;**chromaScale 类补偿对此无效**(补偿是"要求更多彩度",而超出边界意味着显示器物理上发不出该(彩度,亮度)组合,要求更多只会被进一步裁剪)。这是色域体积有限性的必然推论,不是推测。

## 3.3 解决思路(对应原 Q3,已确认正确)

要算出"当前 UI 在彩度可保真范围内的最大亮度",思路如下:

1. **算显示器的色域体积边界**:用**某种色域边界计算算法**(注:具体算法我无法给出可验证的准确出处,不点名——之前报的 "Morovič & Sun, CRA 2000" 已撤回,属记忆错误),在感知空间(如 CIECAM02 的 JCh)里,输入 = 显示器三原色色度 + min/max 亮度(可由 3.1(4) 的元数据得到),输出 = **色域边界的一种离散表示**:即"每个色相 h 下,每个彩度 C 对应的最高可渲染明度 J_max(h, C)"这张表。

2. **把 UI 像素转到感知空间**:每个像素在测试观看条件下算出 (h, C, J)。

3. **逐像素查它的最高可保色明度**:对该像素的 (h, C),从第 1 步的表查 J_max(h, C)。J ≤ J_max → 在色域内,可保色;J > J_max → 超出,保不住。

4. **提亮 = 逐像素提高 J**:均匀放大 RGB 保持色度(h、C 大致不变),J 随亮度上升。**第一个像素的 J 触及其 J_max(h, C) 的那一刻** = 该 UI 的彩度可保真最大亮度;对应的 `uiLumNit` 即所求上限。

5. **超过该亮度**:触及边界的像素超出色域 → 必须降饱和 → 色彩变 → chromaScale 补不回来(已在色域外)。

## 3.4 诚实标注:哪些是思路、哪些待验证

- **3.3 整条思路(色域边界→逐像素 J_max→取 UI 最小)是逻辑 procedure,已确认正确**,不依赖某篇具体论文,保留。
- **"某种色域边界计算算法"**:我无法给出可验证的具体算法/论文出处(之前报的具体人名+论文已撤回,属记忆错误)。落地时需从文献库自行查证一个真实的色域边界计算方法。
- **3.1 各条均有本会话核实来源**,保留。
- **局限(诚实)**:
  - 需要显示器**真实**色域体积(元数据自报值,母版机可信、消费机可能有出入)。
  - CIECAM02 观看条件参数(L_A、surround 等)要按测试环境设对。
  - 逐像素、离线计算量不小。
  - "第一个像素触及边界"(严格 min)可能被个别离群像素主导;实用上可取高百分位(如 99% 像素的 J_max)作可用上限——这是工程选择,不是公式。
  - 本思路只解决"**色彩能否保真(在不在色域内)**"这一约束;主观一致还涉及亮度/对比匹配(Hunt 等),那是另一条约束,要分开处理。

