# 工作记录 — 2026-06-23

## 今日主题：实验数据规整 + HDR 亮度上限扩展 + 窗口化改造 + 编译脚本 + 首轮测试准备

---

## 1. 测试主表 BG=0 行规则化

### 背景

`tests/matching_experiment_data.csv`（12 UI × 10 alpha × 21 BG = 2520 行）中 BG=0 行的填写规则需要统一：BG=0（无 HDR 背景）时 UI 不透明度无需 HDR 调整，`匹配_UI_Alpha` 应等于 `SDR_UI_Alpha`，`匹配_ChromaScale` 与 `备注` 留空。

### 修改

用 Python 脚本处理 120 个 BG=0 行（10 alpha × 12 UI）：

| 列 | 改动 |
|---|---|
| `匹配_UI_Nit` | 保持空（未要求填） |
| `匹配_UI_Alpha` | `N/A` → `SDR_UI_Alpha` 值 |
| `匹配_ChromaScale` | `N/A` → 空 |
| `备注` | `BG=0仅调亮度` → 空 |

非 BG=0 行完全未动。残留检查：`N/A`、`BG=0仅调亮度` 均为 0 处。备份在 `tests/matching_experiment_data.csv.bak`。

> 注：`main.cpp`（遗留死文件，不在编译目标）里的同款逻辑也一并同步，无实际编译效果。

---

## 2. BG Nit 上限改为跟随 Max Nit（最高 2000）

### 背景

HDR 背景亮度滑块 BG Nit 原本硬编码上限 1000 nit，限制了对更高背景亮度的测试。

### 修改

| 文件:行 | 改动 |
|---|---|
| `src/hdr_app.cpp:163` | `DragInt("BG Nit", &bgNit_, 1.0f, 0, 1000)` → 上限改为 `maxNit_`（动态） |
| `src/hdr_app.h:25` | `maxNit_` 默认 `1000` → `2000` |
| `src/main.cpp:1474`、`src/app.h:42` | 死文件同步（无编译效果，保持一致） |

- BG Nit 滑块上限 = 当前 Max Nit 值（始终 ≤ Max Nit）
- Max Nit 滑块本身已可到 2000（`hdr_app.cpp:161`）
- PQ 编码管线本身支持 10000 nit（`pq_convert.frag: y = linearNits / 10000.0`），1000/2000 都只用了部分码值范围，无格式瓶颈

提交：`bf0db03 feat: BG Nit 上限改为跟随 Max Nit（最高 2000）`

---

## 3. HDR 背景亮度正确性验证（分析，未改码）

### 问题

用户怀疑 HDR 窗口背景亮度显示不对（500nit 已很亮、1000nit 无法直视）。验证方法：SDR 端 `srgb_convert.frag` 输出全白 1.0（Eizo 350nit 峰值）vs HDR 端 BG Nit=350nit，对比两边是否一致。

### 结论

验证思路成立，管线代码正确：

- **HDR BG 路径**：`hdr_app.cpp:229` `bgLinear = bgNit / 350` → clear 成纯白（非 18% 灰）→ `pq_convert.frag` `×350 nit` → `linearToPQ(350)` → 显示 350nit ✓
- **SDR 参照**：用户改 `srgb_convert.frag` 输出 1.0 → Eizo 峰值 350nit ✓
- 两边都是 350nit D65 全白，应一致

### 关键前提确认

1. SDR 显示器：Eizo 自带校色仪，自动校准白点+亮度，设为 350nit 峰值（1.0 = 350nit 可保证）
2. HDR 显示器：Sony X3110，已设 SMPTE2084(PQ) + BT.2020 直通
3. 即（1）

→ 验证有效。500/1000nit 全屏刺眼属正常（全屏 1000nit 白本就该刺眼，HDR 通常只在小高光用此亮度）。

> 用户的 `srgb_convert.frag` 验证 hack（输出 1.0）是临时的，仓库里一直是正常版本（`outColor = vec4(encoded, 1.0)`），未提交 hack；后已从本地清除。

---

## 4. 窗口化 + 可拖拽缩放改造

### 背景

原窗口为全屏模式（`glfwCreateWindow(..., monitor, ...)`），无法拖拽/缩放。需求：两窗口可自由拖动改变大小，窗口内按设定亮度显示，UI 始终居中且大小不随窗口变化。

### 现状问题

1. 全屏模式，无法拖拽
2. viewport/scissor 静态烘焙进 pipeline，缩放后不更新（`recreateSwapchain` 不重建 pipeline）
3. UI 缩放用显示器物理尺寸 `physWidth_mm`，窗口化后窗口≠显示器，UI 大小失真
4. UI 已居中（push constant offset=0,0）✓

### 修改

| 文件 | 改动 |
|---|---|
| `src/common.h` | `WindowContext` 加 `pxPerMm`（显示器像素密度）、`framebufferResized`（resize 标志） |
| `src/vulkan_util.cpp` `initVulkanCore` | 全屏→窗口化（decorated+resizable，初始 85% 屏幕居中）；存 `pxPerMm`；注册 framebuffer 尺寸回调 |
| `src/vulkan_util.cpp` 两个 pipeline | viewport/scissor 改为**动态状态**（`VK_DYNAMIC_STATE_VIEWPORT/SCISSOR`） |
| `src/vulkan_util.cpp` `recordUIPass` | 每帧 `vkCmdSetViewport/Scissor`；UI 缩放改用**窗口物理尺寸**（`swapchainExt/pxPerMm`），保持物理尺寸恒定 |
| `src/hdr_app.cpp`/`sdr_app.cpp` `recordConvertPass` | 每帧设动态 viewport/scissor |
| `src/vulkan_util.cpp` `recreateSwapchain` | extent 用实际 framebuffer 尺寸（不再硬编码 `WINDOW_WIDTH/HEIGHT`） |
| `src/hdr_app.cpp`/`src/sdr_app.cpp` 主循环 | 检测 `framebufferResized` 主动重建 swapchain |

### 满足需求

- ✅ 两窗口可自由拖动/缩放（窗口化 + 边框）
- ✅ 窗口内按设定亮度显示（viewport=窗口，PQ/sRGB 管线不变）
- ✅ UI 始终居中（offset=0,0 = viewport 中心 = 窗口中心）
- ✅ UI 大小不随窗口变化（固定物理 mm，`pxPerMm` 计算）

### ⚠️ 待验证

HDR 改窗口化后 PQ 直通取决于系统合成器。索尼 X3110 全屏时 PQ 直通；窗口化后桌面合成器可能对 HDR 窗口 tone-map。需用第 3 节的验证法在窗口化下复验。若亮度变了，可回退到全屏 / 改 borderless fullscreen / `compositeAlpha`。

提交：`2b858ef feat: 窗口化 + 可拖拽缩放，UI 居中且大小恒定`

---

## 5. 跨平台编译脚本

### 背景

每次需手动删 `build/` 重新编译，无统一脚本。

### 新增

| 文件 | 平台 |
|---|---|
| `build.sh` | Linux / macOS（bash） |
| `build.bat` | Windows（cmd，需 VS Developer Command Prompt） |

用法（两平台一致）：
- 无参 / `clean`：全量 Release 重建（删 build，configure，build）
- `inc` / `fast`：增量（不删 build，快速）
- `debug`：全量 Debug 重建

特性：
- 依赖缓存到 `.deps/`（`-DFETCHCONTENT_BASE_DIR`），全量重建时不重复下载 glfw/glm
- Linux 上若装了 Ninja 自动启用（编译更快）
- `.deps/` 已加入 `.gitignore`

已验证：`./build.sh`（clean）与 `./build.sh inc`（增量）均通过，生成 `UI_Vulkan_SDR` / `UI_Vulkan_HDR`。

提交：`46320ab build: 添加跨平台编译脚本 build.sh / build.bat`

---

## 6. 首轮测试 CSV 准备（3 个代表性 UI）

### 背景

12 组 UI × 全矩阵（2520 行）测试量过大。按颜色/亮度/图案/文字维度挑选 3 个代表性 UI 做首轮测试。

### 代表性 UI 选取

基于 PNG 亮度/对比度/色彩分析（PIL 统计 meanLum / std / RGB 均值）：

| 角色 | UI 素材 | 亮度 | 对比度 | 特点 |
|---|---|---|---|---|
| 主参考（密网格） | `2_rgb_1130_769` | 109.4 | 110.2 | 亮+蓝青色，有色彩，判断稳 |
| 验证-暗 | `1_rgb_334_609` | 47.6 | 63.3 | 暗端，中性 |
| 验证-中性 | `5_rgb_328_173` | 94.9 | 114.9 | 中性灰，对比最高 |

> 注：原主参考选 `5_rgb_328_173`，但用户反馈该 UI 只有文字且无色彩，故改用 `2_rgb_1130_769` 作主参考。

### 生成文件

从原表 `matching_experiment_data.csv` 抽取这 3 个 UI 的行，各成独立 CSV（保留 BOM + 表头 + 列顺序与原表一致，便于回填）：

| 文件 | 行数 |
|---|---|
| `tests/test_2_rgb_1130_769.csv` | 210（10 alpha × 21 BG） |
| `tests/test_1_rgb_334_609.csv` | 210 |
| `tests/test_5_rgb_328_173.csv` | 210 |

- BG=0 的 10 行已预填（`匹配_UI_Alpha` = SDR_UI_Alpha，ChromaScale/备注 空）
- BG>0 的 200 行待填：测 `匹配_UI_Nit` + `匹配_UI_Alpha`，ChromaScale/备注 留空
- 迁移回原表：按 `(SDR_UI_Alpha, HDR_BG_Nit, UI素材)` 三列 1:1 匹配回填

### 原表遗留瑕疵

alpha=1.0 在原表中 BG=0 行写成 `1.0`、其余 BG 行写成 `1`（生成脚本不一致）。按原样保留以保证迁移 1:1 —— 实为同一 alpha=1.0。

提交：`93c1a2d test: 添加今晚测试用 CSV（3 个代表性 UI，各全 10 档 alpha）`

---

## 7. Lock 机制问题分析（未修复，已记录）

### 现象

Lock 后拖动 Eff.Alpha，仅当 Lock 前 `UI Lum Nit > BG Nit` 时 `UI Lum Nit` 才跟着变化；若 `UI Lum Nit ≤ BG Nit` 则 `UI Lum Nit` 不变。

### 根因

Lock 用混合亮度保持模型（`hdr_app.cpp:201`）：

```
lumLock = uiLumNit·effAlpha + bgNit·(1−effAlpha)        // 锁定时合成亮度
uiLumNit = (lumLock − bgNit·(1−effAlpha)) / effAlpha    // 改 alpha 反推
```

当 `uiLumNit ≤ bgNit`（锁定时），`lumLock < bgNit`。调小 Eff.Alpha 时 `bgNit·(1−effAlpha)`（背景贡献）超过 `lumLock`，反推得负 → 被第 206 行 `DragFloat` 的 `min=0` 钳到 0 → 卡住。

- `ui < bg`：调小 alpha → uiLumNit 落到 0 后卡住（背景单独已比目标亮，UI 再暗也压不下去）
- `ui == bg`：合成亮度恒等于 bg（与 alpha 无关），uiLumNit 正确地保持 = bg

这是混合亮度模型的固有特性（背景主导时物理上无法再压暗），非单纯代码错。经讨论**暂不修改**，留待后续根据实验需求决定（可选：保持混合亮度显式钳制 / 改回 ui·alpha 保持模型 / 混合）。

---

## 8. 测试反馈（用户首轮测试观察）

1. **测试量精简**：12 组 UI 全矩阵测试项过多，已挑选出 3 个代表性 UI（覆盖颜色、亮度、图案、文字维度）进行首轮测试。

2. **高亮度视觉压迫**：背景亮度超过 500nit 对人眼有很强压迫感，长时间注视影响测试者感官，后续测试数据可能受影响。需注意测试节奏 / 休息 / 数据可信度。

3. **亮度区间差异化采样**：背景亮度 0~300nit 可见较大变化，超过 300nit 后变化逐渐变小。后续考虑按亮度区间区分采样频率（低中亮度区间加密，高亮度区间稀疏）。

4. **背景亮度范围限制**：限制背景亮度变化范围 0~1000nit，更大背景亮度对测试结果影响较小（corner case），暂不测试 >1000nit。

---

## 今日提交汇总

| 提交 | 说明 |
|---|---|
| `bf0db03` | feat: BG Nit 上限改为跟随 Max Nit（最高 2000） |
| `2b858ef` | feat: 窗口化 + 可拖拽缩放，UI 居中且大小恒定 |
| `46320ab` | build: 添加跨平台编译脚本 build.sh / build.bat |
| `93c1a2d` | test: 添加今晚测试用 CSV（3 个代表性 UI，各全 10 档 alpha） |

均在 `chromaScale` 分支，已推送至 `origin`。
