# HDR UI 补偿公式

> 数据来源：tests/0721/，YCbCr 分支，中性色 + 三种饱和色及其三点中点色
> 测试背景：Frame_10890_rotate.png，Sony BVM X3110 (峰值 4000 nit, PQ, BT.2020)

---

## 适用范围

| 参数 | 范围 |
|:----|:----|
| UI 不透明度 α | [0.1, 0.9]（中性色）；[0.5, 0.9]（彩色） |
| 背景亮度 B | [30, 1000] nit |
| 背景类型 | 游戏自然图像（户外、建筑、混合照明） |
| 显示器 | HDR PQ BT.2020，峰值 4000 nit |
| 调节管线 | YCbCr (PQ 域 Y 分量) |

---

## Eff.Alpha（有效不透明度）

### 中性色 UI

```
eff(B, α) = b(α) − (b(α) − g(α)) × exp(−B / τ(α))
```

| 参数 | 公式 | α=0.3 | α=0.5 | α=0.7 | α=0.9 |
|:----|:---|:-----|:-----|:-----|:-----|
| g(α) | 0.809 × α^1.50 | 0.133 | 0.286 | 0.474 | 0.691 |
| b(α) | 0.542 + 0.500×α | 0.692 | 0.792 | 0.892 | 0.992 |
| τ(α) | 113 × (α/0.5)^(−1.0) | 188 | 113 | 81 | 63 |

### 彩色 UI

```
eff_color(B, α) = eff_neutral(B, α) − 0.06 × exp(−B / 200)
```

彩色修正项 `Δ(B) = −0.06 × exp(−B / 200)`：

| B (nit) | 30 | 60 | 100 | 200 | 500 | 1000 |
|:--------|:---|:---|:----|:----|:----|:-----|
| Δ(B) | −0.052 | −0.044 | −0.036 | −0.022 | −0.005 | 0.000 |

---

## Y-Scale（亮度缩放）

```
ys(B) = 1 + 0.188 × ln(1 + B / 403)
```

与颜色无关，与 alpha 无关。

| B (nit) | 30 | 60 | 100 | 150 | 200 | 300 | 500 | 1000 |
|:--------|:---|:---|:----|:----|:----|:----|:----|:-----|
| ys(B) | 1.013 | 1.026 | 1.042 | 1.059 | 1.076 | 1.105 | 1.152 | 1.234 |

---

## 使用方式

对任意 HDR 游戏背景（B nits）和半透明 UI（alpha = α）：

1. **设置 BG Nit = B**（线性亮度目标）
2. **Eff.Alpha**：`eff(B, α)`（中性色）或 `eff(B, α) − 0.06×exp(−B/200)`（彩色）
3. **Y-Scale**：`ys(B)`

---

## 已知限制

1. **α < 0.5 的彩色 UI**：数据不可得，主观匹配在此区间无法收敛
2. **中性色 α < 0.1**：未测试
3. **chroma 连续效应**：全饱和度三色之间的差异在噪声范围内，彩色修正项不区分色度大小
4. **背景依赖性**：仅在 Frame_10890_rotate.png 上验证，其他背景可能有系统性偏差
5. **YCbCr 管线限定**：ICtCp 分支的等效公式待验证
