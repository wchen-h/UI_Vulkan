# 工作记录 — 2026-06-22

## 今日主题：代码审核 + 色彩管线 bug 修复 + 系统过筛

---

## 1. 工程功能理解与阅读顺序

通读 `README.md`、`PROJECT_REVIEW.md`、`CODE_REVIEW_GUIDE.md`、`ISSUES_SUMMARY.md`、`TEST_DESIGN.md` 及全部 live 源码（`vulkan_util.cpp` 1024行、`texture.cpp`、`sdr_app.*`、`hdr_app.*`、5 个 shader）。

确认工程定位：Vulkan SDR/HDR 双可执行文件，用于主观视觉匹配实验。三 pass 管线：UI→linear 中间图→Convert(sRGB/PQ)→ImGui 叠加。线性中间图 `R16G16B16A16_SFLOAT`，归一化 1.0 = paper white = 350 nit。

整理了面向代码审核的四轮阅读顺序（契约层→App 主流程→Vulkan 实现→入口构建），见对话记录。

---

## 2. pq_convert.frag 死代码清理

### 问题

`pq_convert.frag` 中 `if (pc.uHDRSupported < 0.5)` 降级分支不可达：
- `uHDRSupported` 在 `hdr_app.cpp` 写死 `1.0f`
- 无 HDR 显示器时 `chooseHDRSurfaceFormat` 抛异常退出，`drawFrame` 不可达
- 是 legacy 单进程双窗口设计的遗留物

### 修改

| 文件:行 | 改动 |
|---|---|
| `pq_convert.frag:9-12` | `PQPush` 结构体移除 `uHDRSupported` 字段 |
| `pq_convert.frag` main() | 删 if/else，直接 `outColor = vec4(linearToPQ(clamped), 1.0)` |
| `hdr_app.cpp:108` | `createConvertPipeline(..., 8)` → `4` |
| `hdr_app.cpp:134-135` | `float pc[2]` → `float pc`，push constant 8→4 字节 |
| `pq_convert.frag:2` | 头注释 `500 nit white point` → `350 nit paper white` |

push constant 链路对齐：shader 声明 4B ↔ pipeline range 4B ↔ C++ 推送 4B。

---

## 3. BT.709→BT.2020 色域转换 bug（重大）

### 问题发现

用户指出：HDR 通路的 swapchain 是 `HDR10_ST2084`（= BT.2020 primaries + PQ transfer），但整条通路的数据是 BT.709 primaries（来自 sRGB PNG），`pq_convert.frag` 只做了 PQ transfer（1D 逐通道），**没有任何 BT.709→BT.2020 的 3×3 primaries 矩阵转换**。

### 分析确认

数据契约表追踪（HDR 通路 UI 像素）：

| 阶段 | primaries | transfer | 单位 |
|---|---|---|---|
| PNG → sRGB 纹理 → 采样 | BT.709 | sRGB→linear | [0,1] |
| ui.frag 混合 → 线性中间图 | BT.709 | linear | [0,1] |
| pq_convert（修复前） | **仍 BT.709** ✗ | linear→PQ | nit |
| swapchain HDR10_ST2084 | **BT.2020** | PQ | 10bit |

BT.709 gamut ⊂ BT.2020 gamut，显示器把 BT.709 值按 BT.2020 primaries 渲染 → 饱和色**过饱和**，污染 Hunt 效应实验数据（`匹配_ChromaScale` 同时补偿真 Hunt 效应 + gamut 错误）。

### luma 权重是否要改 BT.2020 的论证

**结论：不改，保持 BT.709。** 

原理：CIE 亮度 Y 是空间不变量，但提取 Y 的系数随数据空间而定。转换点之前的数据是 BT.709-linear，必须用 BT.709 系数（`0.2126/0.7152/0.0722`）才能得到正确 Y。改 BT.2020 系数会引入第二处空间错配。转换点之后（pq_convert）无 luma 计算，所以全工程无任何一处需要 BT.2020 luma 权重。

`texture.cpp:214` 的 `lumAvg` 和 `ui.frag:31` 的 chroma 提取保持 BT.709，正确。

### 矩阵计算与验证

用 Python 从 primaries 精确计算 BT.709→BT.2020 矩阵（D65 白点），并验证：
- 三个 BT.709 原色转换后 chromaticity 精确命中（R→0.640/0.330、G→0.300/0.600、B→0.150/0.060）
- 白点保持 D65，灰度不变
- 行和 = 1.0 且全非负 → `[0,1]` BT.709 必映射到 in-gamut `[0,1]` BT.2020，无需额外 clamp

矩阵（GLSL 列主序）：
```
mat3(0.627404, 0.069097, 0.016391,
     0.329283, 0.919540, 0.088013,
     0.043313, 0.011362, 0.895595)
```

> 注：回忆中的"BT.709→BT.2020"矩阵（含负值）是错误记忆，实际验证后全非负（因 BT.709⊂BT.2020，转换矩阵必全非负）。

### 修改

`shaders/pq_convert.frag`：在 main() 中采样后、×350 前，插入 `BT709_TO_BT2020 * linear709`。clamp 移到 BT.2020 空间（显示原生空间）。无 C++ 改动（矩阵是 shader 内 `const mat3`）。

SDR 路径端到端 BT.709，无 gamut 问题，零改动。

---

## 4. 系统过筛：完整问题清单

通读全部 live 源码后输出结构化清单（2 高 / 4 中 / 9 低 / 3 需判定）。逐项处理结果：

### 🔴 HIGH

**H1. HDR 平均亮度显示值错误** — `hdr_app.cpp:154`
`lumAvg * 500.0f`（应为 `PAPER_WHITE_NIT` = 350），显示偏大 1.43 倍。
→ **已修**：改 `(float)PAPER_WHITE_NIT`。

**H2. UI Lum Nit 上限与 MaxNit 不一致，Lock 静默失效** — `hdr_app.cpp:195`
DragInt 上限 1000 vs MaxNit 上限 2000，Lock 公式算出 >1000 被 DragInt 钳位破坏不变量。
→ **已修**：上限提到 2000。MaxNit 语义已澄清为 HDR 窗口最高亮度，UI+BG 混合输出不超 MaxNit（shader `clamp(nitVal, 0, uMaxNit)` 已保证）。

### 🟡 MEDIUM

**M1. RGB/Alpha 尺寸未校验** — `texture.cpp`
→ **不改**：数据源来自游戏抓帧，裁剪坐标固定，尺寸必然对应。

**M2. Lock 锁线性亮度而非感知亮度**
→ **不改（经论证）**：渲染在线性域混合，锁线性混合亮度 ⟺ 锁感知亮度（单调性保证）。感知域混合（PQ/幂律分量混合）锁的是 `f(L_ui)·α + f(L_bg)·(1-α)` ≠ `f(L_ui·α + L_bg·(1-α))`，反而破坏恒定性。线性锁是正确设计。

**M3. Lock 后改 BG Nit 隐式改 UI Nit**
→ **已修**：Lock 后 `ImGui::BeginDisabled(locked_)` 包裹 `DragInt("BG Nit")`。

**M4. Lock 的 `(int)` 截断漂移**
→ **已修**：`uiLumNit_` int→float，全程 float 计算，`DragFloat("%.0f")`（1 nit 步进、整数显示），Lock 公式去 `(int)` 强转。

### 🟢 LOW

| # | 改动 |
|---|---|
| L1 | `pq_convert.frag` 加 `const float PAPER_WHITE_NIT = 350.0;` 命名常量 + 耦合注释，替换裸 `350.0` |
| L2 | `ui.frag:3` 注释 `500.0` → `350.0` |
| L3 | 删除 `texSamplerLin`（创建/销毁/成员全删，从未绑定过任何 descriptor） |
| L4 | debug messenger 存入 `VulkanCore::debugMessenger`，App 析构在 `vkDestroyInstance` 前销毁（修复 Debug 构建泄漏） |

### ✅ 已核查通过

push constant 布局（ui 32B / 两 convert 4B）、CPU/GPU sRGB 解码一致性、SDR 端到端 BT.709、luma 权重空间匹配、vertex 输入布局、quad UV/Y 翻转、线性中间图 format、needsSRGBEncode 双模式、SDR 基准参数、UI 物理尺寸一致性、资源清理完整性。

---

## 5. 本轮全部代码改动汇总

| 文件 | 改动 |
|---|---|
| `shaders/pq_convert.frag` | 删 uHDRSupported 死分支；加 BT.709→BT.2020 矩阵；加 PAPER_WHITE_NIT 命名常量；头注释 500→350 |
| `shaders/ui.frag` | 注释 500→350 |
| `src/common.h` | `VulkanCore` 删 `texSamplerLin`，加 `debugMessenger` |
| `src/vulkan_util.cpp` | 删 `texSamplerLin` 创建；debug messenger 存入 core |
| `src/hdr_app.h` | `uiLumNit_` int→float |
| `src/hdr_app.cpp` | H1 显示值修正；H2 上限 2000；M3 Lock 禁用 BG；M4 全程 float + 去强转 + DragFloat；删 texSamplerLin 销毁；加 debug messenger 销毁 |
| `src/sdr_app.cpp` | 删 texSamplerLin 销毁；加 debug messenger 销毁 |

构建验证：`glslangValidator` + `cmake --build` 全部通过，`UI_Vulkan_SDR`/`UI_Vulkan_HDR` 链接成功。**未提交。**

---

## 6. 待办

### A. 代码鲁棒性（未解决，需修复）

二次针对性核查（除零/越界/空集合）后发现的剩余问题：

- [ ] **[崩溃] 空素材 + Prev/Next 按钮 → 整数除零 UB**
  - 位置：`hdr_app.cpp:157,159`、`sdr_app.cpp:157,159`
  - 原因：Prev/Next 按钮在 `if (!uiPairs_.empty())` 守卫之外，`init()` 仅 `[WARN]` 不退出（`hdr_app.cpp:52`）。若 asset 路径配错/目录空，被试点按钮 → `(0+1) % 0` → UB/崩溃。
  - 注意：正常加载（size ≥ 1）下 Next/Prev 随意循环点击都安全（模运算自动回绕，最后 UI 后点 Next 回到第 0 个）。唯一崩溃前提是空集合。
  - 修复方案：两文件各给 Prev/Next 外加 `if (!uiPairs_.empty())` 守卫，或按钮用 `ImGui::BeginDisabled(uiPairs_.empty())` 包裹。

- [ ] **[UB] `chooseSDRSurfaceFormat` 空列表访问 `formats[0]`** — `vulkan_util.cpp:150-151`
  - 若 `vkGetPhysicalDeviceSurfaceFormatsKHR` 返回 0 个格式，`formats[0]` 越界。低概率但需加空检查。

- [ ] **[泄漏] `uploadTexture` 抛异常时 `rpix`/`apix` 内存泄漏** — `texture.cpp:201-207`
  - 无 RAII 包裹。建议 `std::unique_ptr` + 自定义 deleter。

- [ ] **[风格] `vulkan_util.cpp:251` `std::vector<const char*> devExts;` 缺缩进** — 纯格式。

- [ ] **[风格] `sdr_app.cpp:154,160-161` ImGui 面板重复显示两遍 "BG: 18% gray..."** — 删一处。

### B. Vulkan 同步（运行时验证，静态无法确认）

- [ ] subpass dependency 的 stage/access mask 精度（L7）
  - 三个 render pass 的 `srcStageMask=COLOR_ATTACHMENT_OUTPUT` 对 convert/imgui pass 读取 linearImg（FRAGMENT_SHADER 阶段采样）是否充分，靠 queue 顺序兜底。判断功能上 OK 但未严格证明。
  - **验证手段**：Debug 构建（`kEnableValidation=true`）+ 安装 `VK_LAYER_KHRONOS_validation`，在有显示器的机器上跑一遍，观察 `[VK]` 输出有无 sync/usage warning。这是关闭剩余同步风险的最有效手段。

### C. HDR 真机渲染（运行时验证，静态无法确认）

- [ ] BT.709→BT.2020 矩阵数学已验证，但显示器/OS/驱动是否做二次色域管理（如 Windows DXGI HDR pipeline 自动映射）未知
  - **验证手段**：真机 + 校色仪/参考监视器（Sony HX3100）实测灰场和饱和色块，确认无意外 gamut 压缩
- [ ] R16G16B16A16_SFLOAT（16 位浮点）在暗值区间精度，对 UI 亮度应足够但未实测。低风险。

### D. 实验/文档

- [ ] 正式开始主观匹配实验（`tests/matching_experiment_data.csv` 2520 测试点）
- [ ] 更新 `PROJECT_REVIEW.md` / `CODE_REVIEW_GUIDE.md` 反映本轮管线修复
- [ ] `pq_convert.frag` 的 `PAPER_WHITE_NIT` 与 `common.h` 仍两处独立定义，改值需同步（已知技术债）

---

## 备注

- 本文件为本地工作记录，不纳入 git 追踪
- 前序工作记录见 `WORK_LOG_2026-06-11.md`、`WORK_LOG_2026-06-12.md`
