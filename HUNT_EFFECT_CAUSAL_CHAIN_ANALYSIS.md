# Hunt Effect 因果推导链：逐步分析

本文档对 CIECAM02 中 Hunt Effect 的因果推导链进行逐步严格分析，明确每一步**能推出什么**、**不能推出什么**。

---

## 推导链总览

$$
L_W \;\to\; L_A \;\to\; F_L \;\to\; R_a', G_a', B_a' \;\to\; a, b \;\to\; t \;\to\; C \;\to\; M
$$

**最终结论**：$C \approx$ 常数，$M$ 的增加几乎完全由 $F_L^{0.25}$ 直接驱动。

---

## 步骤 1：$L_W \to L_A \to F_L$

### 定义

- $L_W$：场景白点亮亮度（cd/m²），即显示器的峰值白亮度
- $L_A$：适应场亮度，CIECAM02 取 $L_A \approx L_W / 5$（"中灰适应"假设）
- $F_L$：亮度级因子，完整公式为：

$$
k = \frac{1}{5L_A + 1}, \quad F_L = \frac{1}{5}k^4(5L_A) + \frac{1}{10}(1-k^4)^2(5L_A)^{1/3}
$$

在实际观视条件范围内（$L_A$ 约 50~300 cd/m²），可近似为：

$$
F_L \approx L_A^{1/3}
$$

### 推导

$$
L_W \uparrow \;\to\; L_A \uparrow \;\to\; F_L \uparrow
$$

**结论**：更亮的显示器 $\to$ 更大的 $F_L$。

### 数值示例

| 场景 | $L_W$ (cd/m²) | $L_A \approx L_W/5$ | $F_L$（精确值） |
|------|---------------|---------------------|-----------------|
| SDR | 350 | 70 | ≈ 0.69 |
| HDR | 1000 | 200 | ≈ 0.86 |

比值：$F_{L,\text{HDR}} / F_{L,\text{SDR}} \approx 1.24$

**此步骤无争议。**

---

## 步骤 2：$F_L \to$ 锥体响应压缩 $R_a', G_a', B_a'$

### 压缩函数

CIECAM02 对每个 HPE 锥体响应 $R'$ 应用相同的非线性压缩：

$$
R_a' = \frac{400 \cdot (F_L \cdot R'/100)^{0.42}}{(F_L \cdot R'/100)^{0.42} + 27.13} + 0.1
$$

记 $f(x) = \frac{400 \cdot x^{0.42}}{x^{0.42} + 27.13} + 0.1$，其中 $x = F_L \cdot R'/100$。

### $f(x)$ 的性质

- **单调递增**：$f'(x) > 0$（对所有 $x > 0$）
- **凹函数**：$f''(x) < 0$（在 $x > 0$ 范围内）
- **有界**：$f(x) \to 400.1$（$x \to \infty$）
- **非零截距**：$f(0) = 0.1$

### 推导

$F_L$ 增大 $\to$ 对每个锥体通道，输入 $x_i = F_L \cdot R_i'/100$ 增大。

由于 $f$ 单调递增：$f(k \cdot x_i) > f(x_i)$（$k > 1$），因此 $R_a', G_a', B_a'$ **每个都增大**。

**此步骤无争议：$F_L$ 增大使每个压缩后的锥体响应增大。**

---

## 步骤 3：压缩后响应 $\to$ 对立色信号 $a, b$

### 定义

$$
a = R_a' - \frac{12}{11} G_a' + \frac{1}{11} B_a' = R_a' - \frac{12(G_a' - B_a')}{11}
$$

$$
b = \frac{R_a' + G_a' - 2 B_a'}{9}
$$

### 问题：$F_L$ 增大是否必然使 $|a|, |b|$ 增大？

**不能严格推出。** 原因如下：

#### 3a. 各通道的缩放比不同

$F_L$ 增大 $k$ 倍后，每个锥体通道的压缩输出缩放比为：

$$
r_R = \frac{f(k \cdot x_R)}{f(x_R)}, \quad r_G = \frac{f(k \cdot x_G)}{f(x_G)}, \quad r_B = \frac{f(k \cdot x_B)}{f(x_B)}
$$

由于 $f$ 是非线性函数，且三个通道的输入 $x_R, x_G, x_B$ 不同（对于有色刺激），**三个缩放比互不相同**：$r_R \neq r_G \neq r_B$。

#### 3b. $a$ 的系数有正有负

$$
a_{\text{new}} = r_R \cdot g_R + 0.1 - \frac{12}{11}(r_G \cdot g_G + 0.1) + \frac{1}{11}(r_B \cdot g_B + 0.1)
$$

其中 $g_i = f(x_i) - 0.1$（压缩函数去除常数项后的部分）。整理得：

$$
a_{\text{new}} = r_R \cdot g_R - \frac{12}{11} r_G \cdot g_G + \frac{1}{11} r_B \cdot g_B - \frac{0.1}{11}
$$

由于 $r_R, r_G, r_B$ 各不相同且系数有正有负，**无法提取公因子**，也就无法将 $a_{\text{new}}$ 简洁地表达为 $a_{\text{old}}$ 的简单缩放。

#### 3c. 常数偏移项

压缩函数中的 $+0.1$ 在对 $a$ 的系数求和时产生残余：

$$
0.1 \times \left(1 - \frac{12}{11} + \frac{1}{11}\right) = 0.1 \times \left(-\frac{1}{11}\right) \approx -0.009
$$

此常数项对所有刺激相同，对于 $|a|$ 很小的近中性色刺激，可能影响 $a$ 的变化方向。

#### 3d. 结论

- 对于 $|a|, |b|$ 较大的典型彩色刺激，绝对值**通常**增大（因为缩放比的主导项起主要作用）
- 但这**不是严格必然的**——取决于具体刺激的 $R', G', B'$ 值
- 更重要的是：**即使 $|a|, |b|$ 增大，也不能直接推出 $C$ 增大**（见步骤 4）

---

## 步骤 4：$a, b \to t \to C$

### 定义

$$
t = \frac{\frac{50000}{13} \cdot N_c \cdot N_{cb} \cdot e_t \cdot \sqrt{a^2 + b^2}}{R_a' + G_a' + \frac{21}{20} B_a'}
$$

$$
C = t^{0.9} \cdot \sqrt{\frac{J}{100}} \cdot (1.64 - 0.29^n)^{0.73}
$$

### 关键分析：分子和分母同步变化

$t$ 的结构是**比值**：

| 部分 | 含义 | 随 $F_L$ 增大的变化 |
|------|------|---------------------|
| 分子 $\sqrt{a^2 + b^2}$ | 对立色信号幅度 | 增大（通常） |
| 分母 $R_a' + G_a' + \frac{21}{20}B_a'$ | 压缩后锥体信号加权和（≈无色彩信号） | **也增大** |

**核心机制**：$F_L$ 增大使**所有**压缩后锥体响应增大。分子（色差信号的幅度）和分母（锥体信号总量）的增大比例近似相同。

直观理解：如果将整个色彩信号等比例放大 $s$ 倍，则 $a, b$ 放大 $s$ 倍 $\to$ $\sqrt{a^2+b^2}$ 放大 $s$ 倍，分母也放大 $s$ 倍 $\to$ 比值 $t$ 不变。

实际上由于压缩函数的非线性，各通道缩放比 $r_R, r_G, r_B$ 不完全相同（步骤 3 分析），所以比值 $t$ 会有微小变化。但这种变化是**二阶效应**，量级远小于 1%。

### 数值验证

对典型红色刺激（$R'=50, G'=20, B'=10$）：

| | SDR ($F_L=0.69$) | HDR ($F_L=0.86$) | 比值 |
|---|---|---|---|
| 分子 $\sqrt{a^2+b^2}$ | 26.4 | 32.9 | 1.247 |
| 分母 $\Sigma(\text{cone})$ | 293.8 | 363.8 | 1.238 |
| $t$ | 0.090 | 0.091 | **1.007** |
| $C$（$J=50$ 时） | 16.9 | 17.0 | **1.008** |

$C$ 仅增大 ~0.8%——远小于 $M$ 的 5.6% 变化。

### 结论

$$
C \approx \text{常数}
$$

$C$ 是**相对彩度**（相对于参考白亮度），设计上对亮度水平不敏感。这是 CIECAM02 的设计本意，不是巧合。

---

## 步骤 5：$C, F_L \to M$

### 定义

$$
M = C \cdot F_L^{0.25}
$$

### 推导

由步骤 4，$C \approx$ 常数。因此：

$$
\frac{M_{\text{HDR}}}{M_{\text{SDR}}} \approx \frac{C \cdot F_{L,\text{HDR}}^{0.25}}{C \cdot F_{L,\text{SDR}}^{0.25}} = \left(\frac{F_{L,\text{HDR}}}{F_{L,\text{SDR}}}\right)^{0.25} = \left(\frac{0.86}{0.69}\right)^{0.25} \approx 1.056
$$

$M$ 增加约 **5.6%**，**几乎完全来自 $F_L^{0.25}$ 的直接放大**，而非 $C$ 的变化。

### 结论

**Hunt Effect 体现在 $M$（colorfulness），不是 $C$（chroma）。**

---

## 总结

### 因果链

$$
L_W \uparrow \;\to\; L_A \uparrow \;\to\; F_L \uparrow \;\to\; M = C \cdot F_L^{0.25} \uparrow
$$

其中 $C \approx$ 常数（$t$ 的分子分母同步变化），$M$ 的增加几乎完全由 $F_L^{0.25}$ 直接驱动。

### 结论

提高 HDR 显示的整体亮度（$L_W \uparrow$）会通过 $F_L^{0.25}$ 直接放大 $M$（colorfulness），使 HDR 下的色彩感知比 SDR 更强（约 +5.6%）。

由于 $M = C \cdot F_L^{0.25}$，要保持 $M$ 不变（使 HDR 与 SDR 主观色彩一致），可以通过**降低 $C$** 来补偿 $F_L^{0.25}$ 的放大：

$$
C_{\text{HDR}} \approx \frac{C_{\text{SDR}}}{(F_{L,\text{HDR}} / F_{L,\text{SDR}})^{0.25}} \approx \frac{C_{\text{SDR}}}{1.056}
$$

即需要将 HDR 侧的 $C$ 降低约 5.3%。

---

## 参考文献

- CIE 159:2004, *A Color Appearance Model for Color Management Systems: CIECAM02*
- Fairchild, M.D., *Color Appearance Models*, 3rd ed., Wiley-IS&T, 2013, Chapter 16
