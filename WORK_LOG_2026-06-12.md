# 工作记录 — 2026-06-12

## 今日主题：Hunt Effect 理论研究 + CIECAM02 色貌模型学习

---

## 1. Hunt Effect 论文研读

### 论文信息

- **标题**: Change of Perceived Chroma and Hue of Object Colours at Different Lighting Levels Due to Hunt Effect
- **作者**: Yuki Kawashima & Yoshi Ohno (NIST)
- **来源**: CIE x047:2020 OP01, 5th CIE Symposium on Colour and Visual Appearance
- **链接**: https://files.cie.co.at/x047_2020/x047-OP01.pdf

### 实验方法

- 双视箱分眼观察（haploscopic view）：左眼适应100 lx，右眼适应1000 lx
- 参考色卡放在100 lx侧，20-25个不同彩度/色调的测试色卡放在1000 lx侧
- 22名受试者选择与参考最匹配的测试色卡
- 覆盖红、绿、黄、蓝四种基础色
- 光源CCT: 3000 K 和 6500 K

### 核心发现

- **彩度下降**: 低照度(100 lx)下感知彩度比高照度(1000 lx)低 **8%~15%**（3000 K）和 **8%~12%**（6500 K）
- **绿色彩度下降最大**，黄色最小，红≈黄≈蓝
- **CCT无统计学显著影响** (ANOVA: F(1,21)=1.02, p=0.32)
- **色调偏移**: 低照度下色调角偏移 **-7°~+4°**，红色偏移最大（-7° at 3000 K）
- 红和黄同方向偏移，绿和蓝同方向偏移
- 彩度单位为 **CAM02-UCS** 的 C' = √(a'² + b'²)，百分比是相对变化 ΔC'/C'_ref × 100%

### 对本项目的推论

论文用的是 10:1 照度比（100 lx vs 1000 lx），本项目是 3.4:1 亮度比（350 nit SDR vs 1000 nit HDR）。Hunt Effect 大致随 log 亮度差增长，因此本项目估计彩度差约 **3%~5%**。

**含义**: HDR 显示器上的 UI 元素，即使物理彩度参数完全相同，人眼也会感知它比 SDR 显示器上的更饱和——这正是本项目要测量的现象。

---

## 2. lx 与 nit 的转换关系

- **lx (lux)** = 照度 = 照到表面的光通量密度 (lm/m²)
- **nit (cd/m²)** = 亮度 = 表面发出/反射的光强度密度

对朗伯漫反射表面：

```
L (nit) = E (lx) × ρ (反射率) / π
```

论文中灰色色卡反射率≈50%，因此：
- 100 lx → L ≈ 100 × 0.50 / π ≈ **15.9 nit**
- 1000 lx → L ≈ 1000 × 0.50 / π ≈ **159 nit**

**注意**: 论文测量的是反射光的亮度，本项目测量的是自发光显示器的直接发射亮度——两者都是 nit，但含义不同。

---

## 3. CIECAM02 中 Hunt Effect 的建模机制

CIECAM02 通过三个层级建模 Hunt Effect：

### 第一层：F_L（亮度水平适应因子）出现在锥体响应压缩中

```
k = 1/(5L_A + 1)
F_L = (1/5)k⁴(5L_A) + (1/10)(1-k⁴)²(5L_A)^{1/3}

锥体压缩：L_a' = 400·(F_L·L'/100)^0.42 / [27.13 + (F_L·L'/100)^0.42] + 0.1
```

F_L 出现在压缩函数输入端。L_A 更高 → F_L 更大 → 色差信号 a,b 幅度增大 → 同一色度坐标在高亮度下产生更大色差。

### 第二层：C（彩度）公式中的 √(J/100) — 彩度与明度平方根成正比

```
J = 100·(A/A_w)^{cz}              // 明度(lightness)
C = t^{0.9}·√(J/100)·(1.64-0.29^n)^{0.73}  // 彩度(chroma)
```

高亮度环境 → F_L 更大 → 锥体响应更大 → A 更大 → J 更大 → √(J/100) 更大 → C 更大。

### 第三层：M（色彩度）= C × F_L^{1/4} — 显式 Hunt Effect 放大

```
M = C · F_L^{1/4}                 // 色彩度(colorfulness)
s = 100·√(M/Q)                    // 饱和度(saturation)
```

F_L^{1/4} 同时出现在 M 和 Q 中，因此饱和度 s 基本不随亮度变化——符合定义：saturation = colorfulness relative to own brightness。

### 本项目 SDR vs HDR 对比

| 参数 | SDR (350 nit) | HDR (1000 nit) |
|------|---------------|-----------------|
| L_W | 350 cd/m² | 1000 cd/m² |
| L_A (≈L_W/5) | 70 cd/m² | 200 cd/m² |
| F_L | ~0.57 | ~1.0 |
| √(J/100) | 较小 | 较大 |
| **C (chroma)** | 较小 | 较大 ← Hunt Effect |
| **M (colorfulness)** | 更小 | 更大 ← Hunt Effect × F_L^{1/4} |
| **s (saturation)** | ≈相同 | ≈相同 |

---

## 4. 本工程 UI 图片的彩度计算路径

UI 图片为 sRGB PNG，计算 CIECAM02 彩度需要以下变换链：

```
sRGB (8-bit [0,255])
  → sRGB非线性值 [0,1]                     (除以255)
  → Linear RGB [0,1]                       (sRGB逆OETF: c≤0.04045→c/12.92; else ((c+0.055)/1.055)^2.4)
  → CIE XYZ (D65)                         (3×3矩阵变换)
  → CIECAM02 JCh                           (需要: 适应亮度L_A, 适应白点, 观察条件surround)
```

sRGB → XYZ (D65) 矩阵：

```
| X |   | 0.4124564  0.3575761  0.1804375 | | R_lin |
| Y | = | 0.2126729  0.7151522  0.0721750 | | G_lin |
| Z |   | 0.0193339  0.1191920  0.9503041 | | B_lin |
```

CIECAM02 输入还需：
- **L_A** = 适应亮度 (SDR约70 cd/m², HDR约200 cd/m²)
- **适应白点** = D65 (SDR) 或 display native white (HDR)
- **surround** = "average"（正常观看环境）

CIECAM02 输出 J(明度)、C(彩度)、h(色调角)，再转换为 CAM02-UCS：

```
a' = C·cos(h) / (1 + 0.007·C)
b' = C·sin(h) / (1 + 0.007·C)
C' = √(a'² + b'²)
```

---

## 5. CIE XYZ 三刺激值基础（公式 3.11-3.13）

```
X = k ∫ Φ(λ)·x̄(λ) dλ
Y = k ∫ Φ(λ)·ȳ(λ) dλ
Z = k ∫ Φ(λ)·z̄(λ) dλ
```

- **Φ(λ)**: 刺激的光谱功率分布（自发光体=spectral radiance；反射材料=R(λ)·S(λ)）
- **x̄(λ), ȳ(λ), z̄(λ)**: CIE 1931标准观察者颜色匹配函数（假想原色，ȳ(λ)=V(λ)）
- **k**: 归一化常数（相对色度: k=100/∫S(λ)·ȳ(λ)dλ；绝对色度: k=683 lm/W）
- **Y = 亮度(luminance)**（因为 ȳ(λ)=V(λ)，这是人为设计）
- **X, Z**: 纯色度信息（X和Z原色被设计为零亮度）

---

## 6. 光度学基础（公式 3.6）

```
Φ_V = ∫ Φ(λ)·V(λ) dλ
```

- **Φ(λ)**: 刺激光谱功率分布
- **V(λ)**: CIE 1924明视觉光视效率函数（峰值555nm，V(555)=1.0）
- **Φ_V**: 光度量（辐照度→照度lux；辐亮度→亮度nit）
- V(λ) 是三种锥体按视网膜相对数量加权求和的结果，不是单一锥体响应

---

## 7. 色貌属性定义（Fairchild Ch.4）

6个色貌属性的关系：

| 属性 | 英文 | 中文 | 定义 | 是否依赖亮度水平 |
|------|------|------|------|-----------------|
| Brightness | brightness | 亮度 | 区域发出多少光的绝对感知 | **是**（Hunt Effect） |
| Lightness | lightness | 明度 | 亮度相对于白点的比值 | 近似否 |
| Colorfulness | colorfulness | 色彩度 | 颜色看起来有多浓的绝对感知 | **是**（Hunt Effect） |
| Chroma | chroma | 彩度 | 色彩度相对于白点亮度的比值 | 近似否 |
| Saturation | saturation | 饱和度 | 色彩度相对于自身亮度的比值 | 近似否 |
| Hue | hue | 色调 | 类似红/黄/绿/蓝的感知 | 有微小偏移(Bezold-Brücke) |

**关键关系**：
- Lightness = Brightness / Brightness_white → 相对属性
- Chroma = Colorfulness / Brightness_white → 相对属性
- Saturation = Colorfulness / Brightness_own → 相对属性
- **Colorfulness 的 CIE Note 直接定义了 Hunt Effect**: "For a given chromaticity, this attribute usually increases as the luminance is raised"

---

## 8. Fairchild《Color Appearance Models》3rd ed. 精读路径

| 章节 | 精读/跳读 | 原因 |
|------|----------|------|
| **Ch.4 (4.4-4.9)** | **必须精读** | 色貌属性定义关卡 |
| **Ch.3 (3.6)** | **精读** | XYZ三刺激值定义 |
| **Ch.7 (7.1)** | **精读** | 观视场组件定义 |
| **Ch.10 (10.1, 10.2, 10.4)** | **精读** | CAM定义+CIELAB局限性（无亮度依赖） |
| **Ch.12 (12.3, 12.6-12.9)** | **精读** | Hunt Model: F_L, Q, M, C |
| **Ch.16 (16.2-16.10)** | **精读** | CIECAM02: F_L, J, Q, C, M, s |
| Ch.3 其他 | 跳读 | |
| Ch.7 其他 | 跳读 | |
| Ch.10 (10.3, 10.5) | 跳读 | |
| Ch.12 (12.1, 12.2, 12.11-12.13) | 跳读 | |
| Ch.16 (16.1, 16.11-16.16) | 跳读 | |

---

## TODO

### A. 理论学习（进行中）

- [ ] 精读 Fairchild Ch.4 (4.4-4.9) — 色貌属性定义
- [ ] 精读 Fairchild Ch.3 (3.6) — XYZ三刺激值
- [ ] 精读 Fairchild Ch.7 (7.1) — 观视场组件
- [ ] 精读 Fairchild Ch.10 (10.1, 10.2, 10.4) — CAM定义+CIELAB局限
- [ ] 精读 Fairchild Ch.12 (12.3, 12.6-12.9) — Hunt Model核心公式
- [ ] 精读 Fairchild Ch.16 (16.2-16.10) — CIECAM02核心公式

### B. 项目代码与测试

- [ ] 实现CIECAM02计算工具（输入sRGB→输出JCh），用于量化UI元素的感知彩度差异
- [ ] 验证 MaxNit PQ clamp 在 HDR 显示器上的实际效果（PROJECT_REVIEW.md 中仍未验证）
- [ ] 更新 PROJECT_REVIEW.md 反映当前 split-executable 架构和 350 nit 参考

### C. 设备校准（阻塞项）

- [ ] 同 06-11 工作日志中的设备校准 TODO